"""Голос канала: как писать, чего избегать и проверка текста на «нейросетевые» штампы.
Правила общие для всех текстов бота: мини-фраз, больших постов и заметок."""
import re

from app import db

RULES = """# Голос канала — важнее всех остальных указаний о стиле

Пиши так, как автор канала рассказал бы о находке знакомому в переписке: простыми словами, конкретно, живо. Не как пресс-релиз, не как экскурсовод и не как нейросеть.

Как писать:
- Начинай с самой вещи: что это, где, кто сделал. Без вступлений про архитектуру или искусство вообще.
- Вместо оценок — конкретика: материал, приём, место, история, деталь, которую видно на фото. Хочется написать «впечатляющий» — напиши, что именно там есть.
- Ритм неровный: короткая фраза, потом длинная. Разговорный оборот уместен. Лёгкая ирония — иногда, если материал её выдерживает.
- Цифры — только если в них суть (дом на 18 квадратных метрах, башня 1932 года). Площади, бюджеты, этажность и прочие данные пресс-релиза не пересказывай.
- Заканчивай фактом или деталью. Никакой морали, вывода или красивой формулы в последней фразе.
- Тире — не больше одного на абзац. Чаще подходят точка и запятая.
- Третье лицо, без обращений к читателю, без эмодзи и восклицательных знаков.

Так не пиши — это звучит как нейросеть:
- противопоставление «не X, а Y» и его родственники: «не просто X, а Y», «не столько X, сколько Y», «X — это не Y, это Z», «не X — он Y»;
- финальная фраза-афоризм («Дом, который растёт внутрь.»), обобщения и выводы в конце;
- слова-штампы: выверенный, сдержанный, лаконичный, гармония, баланс, диалог (с контекстом, природой, прошлым), подчёркивает, создаёт ощущение, образ или атмосферу, пространственная логика, растворяется, вписывается, органично, уникальный, впечатляющий, поразительный, завораживающий, игра света и тени, переосмысляет, бросает вызов, стирает границы, на стыке, воплощение, квинтэссенция, манифест, поэтика, медитативный, аутентичный, тишина и покой как абстракция;
- пересказ пресс-релиза: «проект представляет собой», «архитекторы стремились», «по словам бюро», «концепция проекта заключается»;
- перечисления абстракций тройками («свет, материал и тишина»)."""

EXAMPLES = """# Примеры интонации (факты для новых постов бери только из материала)

Большой пост:
<b>Полевая капелла Брата Клауса // Петер Цумтор // Вахендорф, Германия, 2007</b>

Капеллу заказали местные фермеры, и строили её почти сами. Сначала из 112 стволов собрали шалаш, вокруг него слоями залили бетон, а потом дерево подожгли и несколько недель выжигали изнутри.

Снаружи получилась глухая бетонная башня посреди поля. Внутри чёрные стены с отпечатками брёвен, пол из свинца и отверстие в потолке, через которое идёт дождь.

Фразы мини-постов:
Большая волна в Канагаве Хокусая, та самая, с маленькой Фудзи на заднем плане.
Ночной Париж Брассаи: туман, фонари и мокрая брусчатка.
Дом из светлого кирпича под соломенной крышей, окна выходят в сад."""

# (регулярное выражение, как назвать находку автору)
PATTERNS: list[tuple[re.Pattern, str]] = [(re.compile(p, re.I), label) for p, label in [
    (r"\bне\s+(?:просто|только|столько)\b[^.!?;]{1,90}?(?:,|\s—|\s–)\s*(?:а|но|сколько)\b", "«не просто X, а Y»"),
    (r"\bне\b[^.!?;:]{1,60}?(?:,|\s—|\s–)\s*а\s", "«не X, а Y»"),
    (r"\bне\b[^.!?;:,]{1,50}?\s[—–]\s(?:он|она|оно|они|это)\b", "«не X — он Y»"),
    (r"\bэто\s+не\b[^.!?;]{1,60}?(?:,|\s—|\s–)\s*это\b", "«это не X, это Y»"),
    (r"выверенн", "«выверенный»"), (r"сдержанн", "«сдержанный»"), (r"лаконичн", "«лаконичный»"),
    (r"гармони", "«гармония»"), (r"\bбаланс", "«баланс»"), (r"\bдиалог", "«диалог»"),
    (r"подч[её]ркива", "«подчёркивает»"), (r"созда[её]т\s+(?:ощущени|образ|атмосфер)", "«создаёт ощущение»"),
    (r"создают\s+(?:ощущени|образ|атмосфер)", "«создают образ»"), (r"пространственн\w*\s+логик", "«пространственная логика»"),
    (r"раствор[яи]", "«растворяется»"), (r"вписыва", "«вписывается»"), (r"органичн", "«органично»"),
    (r"уникальн", "«уникальный»"), (r"впечатляющ", "«впечатляющий»"), (r"поразительн", "«поразительный»"),
    (r"завораживающ", "«завораживающий»"), (r"игр\w\s+света", "«игра света»"), (r"переосмысл", "«переосмысляет»"),
    (r"броса\w+\s+вызов", "«бросает вызов»"), (r"стира\w+\s+границ", "«стирает границы»"), (r"на\s+стыке", "«на стыке»"),
    (r"воплощени", "«воплощение»"), (r"квинтэссенц", "«квинтэссенция»"), (r"\bманифест", "«манифест»"),
    (r"поэтик", "«поэтика»"), (r"медитативн", "«медитативный»"), (r"аутентичн", "«аутентичный»"),
    (r"представляет\s+собой", "«представляет собой»"), (r"архитекторы\s+стремил", "«архитекторы стремились»"),
    (r"по\s+словам\s+(?:бюро|архитектор|автор)", "«по словам бюро»"), (r"концепция\s+проекта", "«концепция проекта»"),
    (r"чувств\w\s+уюта", "«чувство уюта»"), (r"т[её]пл\w+\s+фактур", "«тёплая фактура»"),
]]


def _plain(text: str) -> str:
    return re.sub(r"<[^>]+>", "", text or "")


def check(text: str, banned: list[str] | None = None) -> list[str]:
    """Найденные штампы и запрещённые автором фразы — пустой список, если текст чистый."""
    s = _plain(text)
    hits = [label for rx, label in PATTERNS if rx.search(s)]
    low = s.lower()
    hits += [f"«{b}»" for b in (banned or []) if b and b.lower() in low]
    for para in s.split("\n\n"):
        if para.count("—") + para.count("–") > 1:
            hits.append("больше одного тире в абзаце")
            break
    return list(dict.fromkeys(hits))


async def banned() -> list[str]:
    return list(await db.get_setting("banned_phrases", []))


async def add_banned(raw: str) -> list[str]:
    """Добавляет одну или несколько фраз (через ; или с новой строки) → что добавилось."""
    have = await banned()
    new = [p.strip(" «»\"'.,") for p in re.split(r"[;\n]", raw)]
    new = [p for p in new if 2 <= len(p) <= 80 and p.lower() not in (h.lower() for h in have)]
    await db.set_setting("banned_phrases", have + new)
    return new


async def remove_banned(i: int) -> str | None:
    have = await banned()
    if not 0 <= i < len(have):
        return None
    gone = have.pop(i)
    await db.set_setting("banned_phrases", have)
    return gone
